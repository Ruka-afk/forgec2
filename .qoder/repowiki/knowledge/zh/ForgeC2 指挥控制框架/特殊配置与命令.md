- **初始化**：首次运行需访问 Web UI 设置管理员密码，系统自动生成 `config.yaml` 及自签名 TLS 证书。
- **Agent 生成**：依赖宿主环境安装 Go 工具链，通过 `GenerateWindowsEXE` 等函数动态编译植入物；支持通过 `GO_BINARY` 环境变量指定编译器路径。
- **部署命令**：`go run ./cmd/server` 或 `docker-compose up --build`。