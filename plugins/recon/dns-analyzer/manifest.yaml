name: dns-analyzer
version: 1.0.0
type: command
description: Analyzes DNS queries, cache records, resolver configuration, and DNS-over-HTTPS usage
author: ""
category: recon
entry: analyze.py
interpreter: python3
timeout: 30
params:
    - name: agent_id
      type: string
      required: false
      description: Specific agent to analyze
