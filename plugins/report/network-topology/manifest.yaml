name: network-topology
version: 1.0.0
type: report
description: Generates network topology visualization showing agent relationships, subnets, and connectivity
author: ""
category: reporting
entry: report.py
interpreter: python3
timeout: 60
params:
    - name: format
      type: string
      required: false
      default: html
      description: Output format (html, markdown, json)
    - name: title
      type: string
      required: false
      default: Network Topology Report
      description: Report title
