version: '3.8'

services:
  forgec2:
    build: .
    container_name: forgec2
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml:ro
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
