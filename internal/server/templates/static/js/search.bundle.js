// ForgeC2 Bundle: search.bundle.js
// Generated: 2026-07-01 23:48:33
// Mode: production

// -------------------------------
// Source: search.js
// -------------------------------

const searchTypeLabels = { agent: 'search.type.agent', listener: 'search.type.listener', credential: 'search.type.credential', bof: 'search.type.bof', user: 'search.type.user', task: 'search.type.task', }; function renderSearchResults(results, query) { const container = document.getElementById('search-results'); if (!container) return; if (!query) { container.innerHTML = `<div class="text-center py-12 text-slate-400 dark:text-slate-500 text-sm" id="search-hint"> <i class="fa-solid fa-magnifying-glass text-2xl mb-2"></i> <p>${__('search.hint')}</p> </div>`; return; } if (!results || results.length === 0) { container.innerHTML = `<div class="text-center py-12 text-slate-400 dark:text-slate-500 text-sm"> <i class="fa-solid fa-inbox text-2xl mb-2"></i> <p>${__('search.no_results')}</p> </div>`; return; } container.innerHTML = results.map(r => { const typeLabel = __(searchTypeLabels[r.type] || r.type); return `<a href="${r.url}" class="flex items-center gap-4 p-4 bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-2xl hover:border-indigo-300 dark:hover:border-indigo-600 hover:shadow-sm transition-all group"> <div class="w-10 h-10 bg-indigo-50 dark:bg-indigo-900/40 rounded-xl flex items-center justify-center text-indigo-600 dark:text-indigo-400 shrink-0"> <i class="fa-solid ${r.icon}"></i> </div> <div class="flex-1 min-w-0"> <div class="flex items-center gap-2"> <span class="font-medium text-slate-900 dark:text-slate-100 group-hover:text-indigo-600 dark:group-hover:text-indigo-400 truncate">${escapeHtml(r.title)}</span> <span class="text-[10px] px-2 py-0.5 bg-slate-100 dark:bg-slate-700 text-slate-500 dark:text-slate-400 rounded-full shrink-0">${typeLabel}</span> </div> <div class="text-xs text-slate-500 dark:text-slate-400 truncate mt-0.5">${escapeHtml(r.subtitle || '')}</div> </div> <i class="fa-solid fa-chevron-right text-slate-300 dark:text-slate-600 group-hover:text-indigo-400"></i> </a>`; }).join(''); } const debouncedSearch = debounce(function(query) { const params = new URLSearchParams({ q: query }); apiFetch('/api/search?' + params.toString()) .then(data => { if (data.success) renderSearchResults(data.results, data.query); }) .catch(() => showToast(__('common.error'), 'error')); }, 250); function runSearch(query) { const q = (query || '').trim(); const url = q ? '/search?q=' + encodeURIComponent(q) : '/search'; if (window.location.pathname === '/search') { history.replaceState(null, '', url); debouncedSearch(q); } else { window.location.href = url; } } document.addEventListener('DOMContentLoaded', function() { const input = document.getElementById('search-page-input'); if (!input) return; input.addEventListener('input', function() { debouncedSearch(input.value.trim()); }); input.addEventListener('keydown', function(e) { if (e.key === 'Enter') { e.preventDefault(); runSearch(input.value); } }); const initial = input.value.trim(); if (initial) debouncedSearch(initial); }); function focusGlobalSearch() { const input = document.getElementById('global-search-input'); if (input) { input.focus(); input.select(); return; } const pageInput = document.getElementById('search-page-input'); if (pageInput) { pageInput.focus(); pageInput.select(); return; } window.location.href = '/search'; } window.focusGlobalSearch = focusGlobalSearch; window.runSearch = runSearch;

