// ForgeC2 Bundle: ops.bundle.js
// Generated: 2026-07-01 23:48:31
// Mode: production

// -------------------------------
// Source: credentials.js
// -------------------------------

let credentialsVirtualList = null; function initCredentialsVirtualList() { const container = document.getElementById('credentials-table-container'); if (!container) return; const tbody = container.querySelector('tbody'); if (!tbody) return; const rows = tbody.querySelectorAll('tr'); if (rows.length > 200) { credentialsVirtualList = new VirtualList({ container: container, threshold: 200, buffer: 10, itemHeight: 55 }); credentialsVirtualList.enable(); setTimeout(() => { if (credentialsVirtualList) { credentialsVirtualList.recalcHeights(); } }, 100); } } function copyCredential(id) { fetch('/credentials/' + id) .then(r => r.json()) .then(cred => { if (cred.password) { navigator.clipboard.writeText(cred.password).then(() => showToast('密码已复制')); } else { showToast('无密码可复制', 'error'); } }).catch(() => showToast('获取凭据失败', 'error')); } function showAddCredModal() { document.getElementById('add-cred-modal').classList.remove('hidden'); } function hideAddCredModal() { document.getElementById('add-cred-modal').classList.add('hidden'); document.getElementById('add-cred-form').reset(); } (function initCredentialsPage() { if (!document.getElementById('add-cred-form')) return; document.getElementById('add-cred-form').addEventListener('submit', function(e) { e.preventDefault(); const fd = new FormData(this); fetch('/credentials/add', { method: 'POST', headers: {'Content-Type': 'application/x-www-form-urlencoded'}, body: new URLSearchParams(fd).toString() }).then(r => r.json()).then(data => { if (data.success) { hideAddCredModal(); showToast('凭据已添加'); setTimeout(() => location.reload(), 800); } else { showToast('添加失败: ' + (data.error || ''), 'error'); } }); }); function deleteCredential(id) { if (!confirm('确定删除此凭据？')) return; fetch('/credentials/' + id, { method: 'DELETE' }) .then(r => r.json()) .then(data => { if (data.success) { showToast('已删除'); setTimeout(() => location.reload(), 500); } }); } function editCredential(id) { fetch('/credentials/' + id) .then(r => r.json()) .then(cred => { document.getElementById('edit-cred-id').value = cred.id; document.getElementById('edit-domain').value = cred.domain || ''; document.getElementById('edit-username').value = cred.username || ''; document.getElementById('edit-tags').value = cred.tags || ''; document.getElementById('edit-notes').value = cred.notes || ''; if (cred.expires_at) { const date = new Date(cred.expires_at); document.getElementById('edit-expires_at').value = date.toISOString().split('T')[0]; } document.getElementById('edit-cred-modal').classList.remove('hidden'); }).catch(() => showToast('获取凭据失败', 'error')); } function hideEditCredModal() { document.getElementById('edit-cred-modal').classList.add('hidden'); } document.getElementById('edit-cred-form').addEventListener('submit', function(e) { e.preventDefault(); const id = document.getElementById('edit-cred-id').value; const fd = new FormData(this); fetch('/credentials/' + id, { method: 'PUT', headers: {'Content-Type': 'application/x-www-form-urlencoded'}, body: new URLSearchParams(fd).toString() }).then(r => r.json()).then(data => { if (data.success) { hideEditCredModal(); showToast('凭据已更新'); setTimeout(() => location.reload(), 800); } else { showToast('更新失败: ' + (data.error || ''), 'error'); } }); }); function toggleConfirmed(id) { fetch('/credentials/' + id + '/confirm', { method: 'POST' }) .then(r => r.json()) .then(data => { if (data.success) { const status = data.confirmed ? '已验证' : '未验证'; showToast('凭据标记为: ' + status); setTimeout(() => location.reload(), 500); } else { showToast('操作失败', 'error'); } }); } function toggleSelectAll() { const checkboxes = document.querySelectorAll('.cred-checkbox'); const selectAll = document.getElementById('select-all'); checkboxes.forEach(cb => cb.checked = selectAll.checked); } function getSelectedIds() { const checkboxes = document.querySelectorAll('.cred-checkbox:checked'); const ids = []; checkboxes.forEach(cb => ids.push(parseInt(cb.dataset.id))); return ids; } function showBatchTagsModal() { const selected = getSelectedIds(); if (selected.length === 0) { showToast('请先选择凭据', 'error'); return; } document.getElementById('batch-tags-modal').classList.remove('hidden'); } function hideBatchTagsModal() { document.getElementById('batch-tags-modal').classList.add('hidden'); document.getElementById('batch-tags-input').value = ''; } document.getElementById('batch-tags-form').addEventListener('submit', function(e) { e.preventDefault(); const tagsInput = document.getElementById('batch-tags-input').value; const tags = tagsInput.split(/[,\n]/).map(t => t.trim()).filter(t => t !== ''); if (tags.length === 0) { showToast('请输入标签', 'error'); return; } const ids = getSelectedIds(); if (ids.length === 0) { showToast('请选择凭据', 'error'); return; } fetch('/credentials/batch/tags', { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({ ids, tags }) }).then(r => r.json()).then(data => { if (data.success) { hideBatchTagsModal(); showToast('标签已添加到 ' + data.count + ' 条凭据'); setTimeout(() => location.reload(), 800); } else { showToast('操作失败: ' + (data.error || ''), 'error'); } }); }); function filterByTag(tag) { const currentParams = new URLSearchParams(window.location.search); if (currentParams.get('tag') === tag) { currentParams.delete('tag'); } else { currentParams.set('tag', tag); } window.location.search = currentParams.toString(); } function applyFilters() { const search = document.getElementById('search-input').value; const expiry = document.getElementById('expiry-filter').value; const confirmed = document.getElementById('confirmed-filter').value; const params = new URLSearchParams(); if (search) params.set('search', search); if (expiry) params.set('expiry', expiry); if (confirmed) params.set('confirmed', confirmed); window.location.search = params.toString(); } function clearFilters() { window.location.search = ''; } function showExportOptions() { document.getElementById('export-options-modal').classList.remove('hidden'); } function hideExportOptions() { document.getElementById('export-options-modal').classList.add('hidden'); } function exportCredentials() { const scope = document.querySelector('input[name="export_scope"]:checked').value; const currentParams = new URLSearchParams(window.location.search); const urlParams = new URLSearchParams(); if (scope === 'expired') { urlParams.set('expiry', 'expired'); } else if (scope === 'current') { const tag = currentParams.get('tag'); const expiry = currentParams.get('expiry'); const search = currentParams.get('search'); const confirmed = currentParams.get('confirmed'); if (tag) urlParams.set('tag', tag); if (expiry) urlParams.set('expiry', expiry); if (search) urlParams.set('search', search); if (confirmed) urlParams.set('confirmed', confirmed); } window.location.href = '/credentials/export?' + urlParams.toString(); hideExportOptions(); } document.getElementById('search-input').addEventListener('keypress', function(e) { if (e.key === 'Enter') { applyFilters(); } }); document.addEventListener('DOMContentLoaded', function() { initCredentialsVirtualList(); }); })();

// -------------------------------
// Source: tasks.js
// -------------------------------

let refreshTimer = null; let tasksVirtualList = null; function initTasksVirtualList() { const container = document.getElementById('tasks-table-container'); if (!container) return; const tbody = container.querySelector('tbody'); if (!tbody) return; const rows = tbody.querySelectorAll('tr'); if (rows.length > 200) { tasksVirtualList = new VirtualList({ container: container, threshold: 200, buffer: 10, itemHeight: 65 }); tasksVirtualList.enable(); setTimeout(() => { if (tasksVirtualList) { tasksVirtualList.recalcHeights(); } }, 100); } } function autoRefresh() { const pending = document.querySelectorAll('.task-row[data-status="pending"]'); if (pending.length > 0) { document.getElementById('live-status').classList.remove('hidden'); fetch(window.location.href, { headers: { 'X-Requested-With': 'XMLHttpRequest' } }) .then(r => r.text()) .then(html => { const parser = new DOMParser(); const doc = parser.parseFromString(html, 'text/html'); const newTbody = doc.getElementById('tasks-tbody'); const oldTbody = document.getElementById('tasks-tbody'); if (newTbody && oldTbody) { oldTbody.innerHTML = newTbody.innerHTML; if (tasksVirtualList && tasksVirtualList.enabled) { tasksVirtualList.disable(); tasksVirtualList = null; initTasksVirtualList(); } } const stillPending = document.querySelectorAll('.task-row[data-status="pending"]'); if (stillPending.length === 0) { document.getElementById('live-status').classList.add('hidden'); if (refreshTimer) { clearTimeout(refreshTimer); refreshTimer = null; } } else { refreshTimer = setTimeout(autoRefresh, 5000); } }) .catch(() => { refreshTimer = setTimeout(autoRefresh, 5000); }); } else { document.getElementById('live-status').classList.add('hidden'); if (refreshTimer) { clearTimeout(refreshTimer); refreshTimer = null; } } } document.addEventListener('DOMContentLoaded', function() { initTasksVirtualList(); const pending = document.querySelectorAll('.task-row[data-status="pending"]'); if (pending.length > 0) { refreshTimer = setTimeout(autoRefresh, 5000); } }); function exportTasks() { const a = document.createElement('a'); a.href = '/tasks/export'; a.download = 'forgec2_tasks_' + new Date().toISOString().split('T')[0] + '.csv'; document.body.appendChild(a); a.click(); document.body.removeChild(a); showToast && showToast('正在导出任务报告...'); } function rerunTask(agentId, taskId) { fetch(`/agents/${agentId}/task/${taskId}/rerun`, { method: 'POST' }) .then(r => r.json()) .then(data => { if (data.success) { showToast('任务已重新创建，等待 Implant 执行'); setTimeout(() => location.reload(), 1500); } else { showToast('重跑失败: ' + (data.error || '未知错误'), 'error'); } }) .catch(err => showToast('请求失败: ' + err, 'error')); } function retryAllFailed() { if (!confirm('确认要重试所有失败任务？')) return; const rows = document.querySelectorAll('.task-row[data-status="failed"]'); let count = 0; rows.forEach(row => { const agentId = row.dataset.agentId; const taskId = row.dataset.taskId; if (agentId && taskId) { fetch(`/agents/${agentId}/task/${taskId}/rerun`, { method: 'POST' }) .then(r => r.json()) .then(data => { if (data.success) count++; }); } }); showToast(`正在重试 ${rows.length} 个失败任务...`); setTimeout(() => location.reload(), 2000); } // Keyboard shortcut: Ctrl+F to focus search document.addEventListener('keydown', function(e) { if ((e.ctrlKey || e.metaKey) && e.key === 'f') { const searchInput = document.querySelector('input[name="q"]'); if (searchInput && !e.target.closest('input, textarea')) { e.preventDefault(); searchInput.focus(); } } });

// -------------------------------
// Source: audit.js
// -------------------------------

let currentPage = 1; let totalPages = 1; let totalRecords = 0; const pageSize = 50; const virtualThreshold = 200; let useVirtualScroll = false; let allLogs = []; let auditVirtualList = null; const debouncedLoad = debounce(loadLogs, 300); function loadLogs() { const search = document.getElementById('search-input').value; const action = document.getElementById('action-filter').value; const user = document.getElementById('user-filter').value; if (useVirtualScroll) { loadAllLogsForVirtualScroll(search, action, user); } else { loadLogsPaginated(search, action, user); } } function loadLogsPaginated(search, action, user) { apiFetch('/audit/logs?page=' + currentPage + '&pageSize=' + pageSize + '&search=' + encodeURIComponent(search) + '&action=' + action + '&user=' + encodeURIComponent(user)) .then(data => { if (data.success) { totalRecords = data.total; totalPages = Math.ceil(totalRecords / pageSize); if (totalRecords > virtualThreshold && !useVirtualScroll) { useVirtualScroll = true; loadAllLogsForVirtualScroll(search, action, user); return; } renderLogs(data.data); updatePagination(); } else { showError(data.error || __('Load failed')); } }).catch(err => showError(__tf('Network error: {0}', err))); } function loadAllLogsForVirtualScroll(search, action, user) { apiFetch('/audit/logs?page=1&pageSize=10000&search=' + encodeURIComponent(search) + '&action=' + action + '&user=' + encodeURIComponent(user)) .then(data => { if (data.success) { allLogs = data.data || []; totalRecords = data.total; totalPages = 1; initAuditVirtualList(); updatePagination(); } else { showError(data.error || __('Load failed')); } }).catch(err => showError(__tf('Network error: {0}', err))); } function initAuditVirtualList() { const container = document.getElementById('audit-table-container'); if (!container) return; if (auditVirtualList) { auditVirtualList.destroy(); auditVirtualList = null; } const table = container.querySelector('table'); if (!table) return; const tbody = table.querySelector('tbody'); if (tbody) { tbody.innerHTML = allLogs.map((log, index) => renderLogRow(log, index)).join(''); } auditVirtualList = new VirtualList({ container: container, items: allLogs, threshold: virtualThreshold, buffer: 10, itemHeight: 48, renderItem: function(log, index) { return renderLogRow(log, index); } }); auditVirtualList.enable(); const pagination = document.querySelector('.bg-slate-50.border-t.border-slate-200.px-6.py-4'); if (pagination) pagination.style.display = 'none'; const totalCount = document.getElementById('total-count'); if (totalCount) totalCount.textContent = __tf('{0} records (virtual scrolling enabled)', totalRecords); setTimeout(() => { if (auditVirtualList) auditVirtualList.recalcHeights(); }, 100); } function renderLogRow(log, index) { return '<tr class="hover:bg-slate-50 transition-colors"><td class="py-3 px-4 text-sm text-slate-600">' + formatTime(log.created_at) + '</td><td class="py-3 px-4 text-sm"><span class="px-2 py-1 bg-indigo-100 text-indigo-700 rounded-lg text-xs">' + escapeHtml(log.user) + '</span></td><td class="py-3 px-4 text-sm text-slate-600 font-mono">' + escapeHtml(log.ip) + ' || '-'</td><td class="py-3 px-4 text-sm"><span class="px-2 py-1 ' + getActionClass(log.action) + ' rounded-lg text-xs">' + getActionLabel(log.action) + '</span></td><td class="py-3 px-4 text-sm text-slate-600">' + escapeHtml(log.resource) + ' || '-'</td><td class="py-3 px-4 text-sm text-slate-600 font-mono">' + (log.agent_id ? log.agent_id.substring(0, 8) + '...' : '-') + '</td><td class="py-3 px-4 text-sm"><span class="flex items-center">' + (log.success ? '<i class="fa-solid fa-check-circle text-emerald-500 mr-1"></i><span class="text-emerald-600">' + __('Success') + '</span>' : '<i class="fa-solid fa-x-circle text-red-500 mr-1"></i><span class="text-red-600">' + __('Failure') + '</span>') + '</span></td><td class="py-3 px-4 text-sm text-slate-500 max-w-xs truncate" title="' + escapeHtml(log.details || log.error || '-') + '">' + escapeHtml(log.details || log.error || '-') + '</td></tr>'; } function renderLogs(logs) { const tbody = document.getElementById('log-table-body'); if (logs.length === 0) { tbody.innerHTML = '<tr><td colspan="8" class="py-16 text-center text-slate-400">' + __('No audit records') + '</td></tr>'; return; } tbody.innerHTML = logs.map(log => renderLogRow(log)).join(''); } function formatTime(timeStr) { if (!timeStr) return '-'; const date = new Date(timeStr); return date.toLocaleString(undefined, { year: 'numeric', month: '2-digit', day: '2-digit', hour: '2-digit', minute: '2-digit', second: '2-digit' }); } function getActionClass(action) { const classes = { 'login': 'bg-emerald-100 text-emerald-700', 'logout': 'bg-slate-100 text-slate-700', 'send_command': 'bg-indigo-100 text-indigo-700', 'command_result': 'bg-teal-100 text-teal-700', 'batch_command': 'bg-violet-100 text-violet-700', 'agent_action': 'bg-blue-100 text-blue-700', 'kill_agent': 'bg-red-100 text-red-700', 'delete_agent': 'bg-rose-100 text-rose-700', 'request_screenshot': 'bg-sky-100 text-sky-700', 'request_screenshot_window': 'bg-sky-100 text-sky-700', 'request_ps': 'bg-amber-100 text-amber-700', 'generate': 'bg-purple-100 text-purple-700', 'screen_monitor_start': 'bg-cyan-100 text-cyan-700', 'screen_monitor_stop': 'bg-orange-100 text-orange-700', 'file_ls': 'bg-slate-100 text-slate-700', 'file_delete': 'bg-red-100 text-red-700', 'file_read': 'bg-blue-100 text-blue-700', 'file_upload_exfil': 'bg-emerald-100 text-emerald-700', 'file_download_exfil': 'bg-emerald-100 text-emerald-700' }; return classes[action] || 'bg-gray-100 text-gray-700'; } function getActionLabel(action) { const labels = { 'login': __('Login'), 'logout': __('Logout'), 'send_command': __('Send Command'), 'command_result': __('Command Result'), 'batch_command': __('Batch Command'), 'agent_action': __('Agent Action'), 'kill_agent': __('Kill Agent'), 'delete_agent': __('Delete Agent'), 'request_screenshot': __('Request Screenshot'), 'request_screenshot_window': __('Request Window Screenshot'), 'request_ps': __('Request Process List'), 'generate': __('Generate'), 'screen_monitor_start': __('Start Monitor'), 'screen_monitor_stop': __('Stop Monitor'), 'file_ls': __('File List'), 'file_delete': __('File Delete'), 'file_read': __('File Read'), 'file_upload_exfil': __('File Upload (Inbound)'), 'file_download_exfil': __('File Download (Exfil)') }; return labels[action] || action; } function updatePagination() { document.getElementById('total-count').textContent = __tf('{0} records', totalRecords); document.getElementById('current-page').textContent = currentPage; document.getElementById('prev-page').disabled = currentPage <= 1; document.getElementById('next-page').disabled = currentPage >= totalPages; const start = (currentPage - 1) * pageSize + 1; const end = Math.min(currentPage * pageSize, totalRecords); document.getElementById('page-info').textContent = start + '-' + end; } function prevPage() { if (currentPage > 1) { currentPage--; loadLogs(); } } function nextPage() { if (currentPage < totalPages) { currentPage++; loadLogs(); } } function applyFilters() { currentPage = 1; useVirtualScroll = false; allLogs = []; if (auditVirtualList) { auditVirtualList.destroy(); auditVirtualList = null; } const pagination = document.querySelector('.bg-slate-50.border-t.border-slate-200.px-6.py-4'); if (pagination) pagination.style.display = ''; loadLogs(); } function resetFilters() { document.getElementById('search-input').value = ''; document.getElementById('action-filter').value = ''; document.getElementById('user-filter').value = ''; currentPage = 1; useVirtualScroll = false; allLogs = []; if (auditVirtualList) { auditVirtualList.destroy(); auditVirtualList = null; } const pagination = document.querySelector('.bg-slate-50.border-t.border-slate-200.px-6.py-4'); if (pagination) pagination.style.display = ''; loadLogs(); } function exportLogs() { const search = document.getElementById('search-input').value; const action = document.getElementById('action-filter').value; const user = document.getElementById('user-filter').value; apiFetch('/audit/logs?page=1&pageSize=' + totalRecords + '&search=' + encodeURIComponent(search) + '&action=' + action + '&user=' + encodeURIComponent(user)) .then(data => { if (data.success) { const csv = convertToCSV(data.data); downloadCSV(csv); } else { showError(data.error || __('Export failed')); } }).catch(err => showError(__tf('Network error: {0}', err))); } function convertToCSV(logs) { const headers = [__('Time'), __('User'), __('IP'), __('Action'), __('Resource'), __('Agent'), __('Status'), __('Details')]; const rows = logs.map(log => [formatTime(log.created_at), log.user, log.ip || '', getActionLabel(log.action), log.resource || '', log.agent_id || '', log.success ? __('Success') : __('Failure'), log.details || log.error || '']); return [headers.join(','), ...rows.map(row => row.map(cell => '"' + cell.replace(/"/g, '""') + '"').join(','))].join('\n'); } function downloadCSV(content) { const blob = new Blob(['\uFEFF' + content], { type: 'text/csv;charset=utf-8;' }); const link = document.createElement('a'); link.href = URL.createObjectURL(blob); link.download = 'audit_logs_' + new Date().toISOString().split('T')[0] + '.csv'; link.click(); } function escapeHtml(text) { if (!text) return ''; const div = document.createElement('div'); div.appendChild(document.createTextNode(text)); return div.innerHTML; } function showError(message) { const container = document.getElementById('toast-container'); const toast = document.createElement('div'); toast.className = 'px-4 py-3 rounded-2xl shadow-xl flex items-center gap-x-3 text-sm bg-red-100 border border-red-300 text-red-700'; toast.innerHTML = '<i class="fa-solid fa-exclamation-circle"></i>'; const msgSpan = document.createElement('span'); msgSpan.textContent = message; toast.appendChild(msgSpan); container.appendChild(toast); setTimeout(() => { toast.style.transition = 'all 0.3s ease'; toast.style.opacity = '0'; setTimeout(() => toast.remove(), 300); }, 3000); } (function initAuditPage() { const searchInput = document.getElementById('search-input'); if (!searchInput || !document.getElementById('action-filter')) return; searchInput.addEventListener('keyup', function(e) { if (e.key === 'Enter') applyFilters(); }); searchInput.addEventListener('input', function() { debouncedLoad(); }); loadLogs(); })();

